import type { NextConfig } from "next";

// Comment out all of nextConfig, if you want to run locally with npm run dev
const nextConfig: NextConfig = {
  output: 'export',
  basePath: '',
  assetPrefix: 'https://kaichuuu.github.io',
};

export default nextConfig;